import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  turbopack: {
    root: "/Users/mac/Arc Synthesis/luminary_co/project CR/dashboard/project-cr-app"
  }
};

export default nextConfig;
